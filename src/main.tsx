import React from 'react'
import ReactDOM from 'react-dom'
import {App} from './App'
import { CartPokemon } from './components/CartaoPokemon';

ReactDOM.render(
  <React.StrictMode>
    <CartPokemon />
  </React.StrictMode>,
  document.getElementById('root')
)
